on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to give writing advise
    steps:
      - name: proselint action
        id: proselint
        uses: chengjackjelly/proselint-docker-action@v1

